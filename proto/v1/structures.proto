syntax = 'proto3';

package beget.statistics.v1.structures;

// Значение за определенный момент времени
message StatisticsSet {
  // Время (RFC3339)
  string datetime = 1;
  // Значение
  double value = 2;
}